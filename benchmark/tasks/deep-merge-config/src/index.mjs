import { isPlainObject } from './is-plain-object.mjs';
import { mergeObjects } from './merge-objects.mjs';

export function deepMergeConfig(baseConfig, overrideConfig) {
  throw new Error('TODO');
}

export { isPlainObject, mergeObjects };
