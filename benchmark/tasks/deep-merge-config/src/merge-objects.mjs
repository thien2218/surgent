import { cloneSafeValue, isUnsafeKey } from './clone-safe-value.mjs';
import { isPlainObject } from './is-plain-object.mjs';

export function mergeObjects(baseConfig, overrideConfig) {
  throw new Error('TODO');
}

export { isPlainObject };
