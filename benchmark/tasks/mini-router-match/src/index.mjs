import { compilePattern } from './compile-pattern.mjs';
import { matchCompiledPattern } from './match-pattern.mjs';
import { normalizePath } from './normalize-path.mjs';

export function matchBestRoute(routePatterns, requestPath) {
  throw new Error('TODO');
}

export { compilePattern, matchCompiledPattern, normalizePath };
