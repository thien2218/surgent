import { directionDeltaByName, isDirectionName, oppositeDirectionByName } from './constants.mjs';
import { findNextFoodPosition } from './food.mjs';

export function createGameState(config) {
  throw new Error('TODO');
}

export function setDirection(gameState, nextDirection) {
  throw new Error('TODO');
}

export function tickGame(gameState) {
  throw new Error('TODO');
}

export { directionDeltaByName, isDirectionName, oppositeDirectionByName };
