Review target below. Inspect changed code and related code needed to verify behavior. Do not change files. Report only evidence-backed, actionable findings.

Return markdown with exact sections:

# Review: [title]

## Verdict
`APPROVE` | `REQUEST_CHANGES` | `NEEDS_INFO`  
One-line reason.

## Findings
If none, write `No actionable issues found.`

For each finding:

### [<SEVERITY>] <title>
- Evidence: `<file>:<line start> - <line end>` or `<diff hunk>`
- Impact:
- Recommended fix:

## Missing context
- ...
(Only if needed to validate uncertain areas; otherwise omit section.)

## Checks performed
- `<tool or method used to inspect changes>`

Use BLOCKER, HIGH, MEDIUM, or LOW severity. Sort findings by severity.
